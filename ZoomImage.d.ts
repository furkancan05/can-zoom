import React from "react";
import { ZoomImageParams } from "./zoomImageTypes";
/**
 * Component that you can zoom images with mouse hover.
 */
export default function ZoomImage({ scale, maxScale, width, height, backgroundColor, imagePath, objectFit, cursor, borderStyle, borderWidth, borderColor, borderRadius, }: ZoomImageParams): React.JSX.Element;
